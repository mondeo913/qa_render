<!doctype html><html lang="es" data-siget-theme="auto"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="light dark"><title>Ingresar · {{ config('siget.name') }}</title><script>(()=>{const t=localStorage.getItem('siget-theme')||'auto';document.documentElement.dataset.bsTheme=t==='auto'?(matchMedia('(prefers-color-scheme:dark)').matches?'dark':'light'):t;})();</script>@php($m=file_exists(public_path('build/manifest.json'))?json_decode(file_get_contents(public_path('build/manifest.json')),true):[]) @if($c=$m['resources/css/app.scss']['file']??null)<link rel="stylesheet" href="{{ '/build/'.ltrim($c,'/') }}">@endif @if($j=$m['resources/js/app.js']['file']??null)<script type="module" src="{{ '/build/'.ltrim($j,'/') }}"></script>@endif</head><body class="siget-body"><div class="siget-auth-page"><section class="siget-auth-brand-panel"><div class="siget-auth-logo"><span class="siget-brand-mark">S</span><span><strong>{{ config('siget.name') }}</strong><small>{{ config('siget.subtitle') }}</small></span></div><h1>Gestión institucional clara, segura y trazable.</h1><p>Administre pautas, calendario inteligente, cargas programadas, evidencias, revisión institucional y cierre desde una sola plataforma.</p><div class="siget-auth-benefits"><div class="siget-auth-benefit"><i class="bi bi-shield-check"></i><span><strong>Seguridad por roles</strong><small>Acceso segmentado por dependencia y dirección.</small></span></div><div class="siget-auth-benefit"><i class="bi bi-calendar2-check"></i><span><strong>Calendario inteligente</strong><small>Fechas generadas desde las pautas confirmadas.</small></span></div><div class="siget-auth-benefit"><i class="bi bi-graph-up-arrow"></i><span><strong>Seguimiento operativo</strong><small>Kanban, alertas, auditoría y repositorio.</small></span></div></div></section><main class="siget-auth-form-panel"><div class="siget-auth-card"><div class="d-flex justify-content-between mb-4"><div><h2 class="mb-1">Bienvenido</h2><p class="text-secondary mb-0">Ingrese con su cuenta institucional.</p></div><div class="dropdown"><button class="siget-icon-button" data-bs-toggle="dropdown"><i class="bi bi-circle-half" data-theme-icon></i></button><div class="dropdown-menu dropdown-menu-end"><button class="dropdown-item" data-theme-value="light">Claro</button><button class="dropdown-item" data-theme-value="dark">Oscuro</button><button class="dropdown-item" data-theme-value="auto">Automático</button></div></div></div>@if(session('status'))<div class="alert alert-info">{{ session('status') }}</div>@endif @if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('login.store') }}">@csrf<label class="form-label">Correo institucional</label><input class="form-control mb-3" type="email" name="email" value="{{ old('email') }}" autocomplete="username" required autofocus><div class="d-flex justify-content-between"><label class="form-label">Contraseña</label><a href="{{ route('password.request') }}" class="small text-decoration-none">¿Olvidó su contraseña?</a></div><div class="siget-password-wrap mb-3"><input id="password" class="form-control" type="password" name="password" autocomplete="current-password" required><button type="button" class="siget-password-toggle" data-password-toggle="password"><i class="bi bi-eye"></i></button></div><div class="form-check mb-4"><input class="form-check-input" type="checkbox" name="remember" value="1" id="remember"><label class="form-check-label" for="remember">Mantener sesión</label></div><button class="btn btn-primary w-100 py-2"><i class="bi bi-box-arrow-in-right me-2"></i>Ingresar a SIGET</button></form><div class="siget-auth-security"><i class="bi bi-lock me-1"></i>El acceso y las operaciones relevantes quedan registrados.</div></div></main></div></body></html>
