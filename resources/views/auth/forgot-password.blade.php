<!doctype html><html lang="es" data-siget-theme="auto"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Recuperar contraseña · SIGET</title><script>(()=>{const t=localStorage.getItem('siget-theme')||'auto';document.documentElement.dataset.bsTheme=t==='auto'?(matchMedia('(prefers-color-scheme:dark)').matches?'dark':'light'):t;})();</script>@php($m=file_exists(public_path('build/manifest.json'))?json_decode(file_get_contents(public_path('build/manifest.json')),true):[]) @if($c=$m['resources/css/app.scss']['file']??null)<link rel="stylesheet" href="{{ '/build/'.ltrim($c,'/') }}">@endif @if($j=$m['resources/js/app.js']['file']??null)<script type="module" src="{{ '/build/'.ltrim($j,'/') }}"></script>@endif</head><body class="siget-body"><div class="siget-auth-page"><section class="siget-auth-brand-panel"><div class="siget-auth-logo"><span class="siget-brand-mark">S</span><span><strong>SIGET</strong><small>{{ config('siget.subtitle') }}</small></span></div><h1>Recupere el acceso de forma segura.</h1><p>Se enviará un enlace temporal al correo registrado en SIGET.</p></section><main class="siget-auth-form-panel"><div class="siget-auth-card"><h2>Recuperar contraseña</h2><p class="text-secondary">Escriba su correo institucional.</p>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif @if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('password.email') }}">@csrf<label class="form-label">Correo institucional</label><input class="form-control mb-3" type="email" name="email" value="{{ old('email') }}" required autofocus><button class="btn btn-primary w-100">Enviar enlace</button></form><a href="{{ route('login') }}" class="btn btn-link w-100 mt-3">Volver al inicio</a></div></main></div></body></html>
