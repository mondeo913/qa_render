@extends('layouts.app')
@section('title','Dashboard SIGET')
@section('page-title', $presentation['title'] ?? 'Dashboard ejecutivo')
@section('page-subtitle', $presentation['subtitle'] ?? 'Control ejecutivo SIGET')
@section('content')
@php
$k = $analytics['kpis'] ?? [];
$filters = $filters ?? [];
$monthly = $analytics['monthly_trend'] ?? [];
$funnel = $analytics['deliverable_funnel'] ?? [];
$unitPerformance = $analytics['unit_performance'] ?? [];
$agencyPerformance = $analytics['agency_performance'] ?? [];
$evidenceFunnel = $analytics['evidence_funnel'] ?? [];
$selectedUnit = (string)($filters['organizational_unit_id'] ?? '');
@endphp
<section class="siget-hero mb-4"><span class="badge text-bg-primary mb-2">{{ $presentation['scope'] ?? 'Vista institucional' }}</span><h2>Dashboard ejecutivo SIGET</h2><p>Control de avance, carga operativa, riesgo, oportunidad y trazabilidad.</p></section>
<form method="GET" class="card siget-card mb-4"><div class="card-header"><div><h2>Contexto de análisis</h2><p>Define el universo antes de interpretar los resultados.</p></div></div><div class="card-body row g-3 align-items-end">
<div class="col-xl-3 col-md-6"><label class="form-label">Dependencia</label><select name="agency_id" class="form-select"><option value="">Todas las dependencias</option>@foreach($agencies as $agency)<option value="{{ $agency->id }}" @selected(($filters['agency_id'] ?? null) == $agency->id)>{{ $agency->name }}</option>@endforeach</select></div>
<div class="col-xl-3 col-md-6"><label class="form-label">Dirección / unidad</label><select name="organizational_unit_id" class="form-select"><option value="">Todas las direcciones / unidades</option>@foreach($units as $unit)@php $unitIds=implode(',', $unit->filter_unit_ids ?? [$unit->id]); @endphp<option value="{{ $unitIds }}" @selected($selectedUnit === $unitIds)>{{ $unit->name }}</option>@endforeach</select></div>
<div class="col-xl-2 col-md-4"><label class="form-label">Estado</label><select name="status" class="form-select"><option value="">Todos</option>@foreach(['PROGRAMADA','ABIERTA','EN_CAPTURA','PARCIALMENTE_ENTREGADA','ENTREGADA','EN_REVISION_INSTITUCIONAL','OBSERVADA','VALIDADA','VALIDADO_Y_CERRADO','VENCIDA','REPROGRAMADA'] as $status)<option value="{{ $status }}" @selected(($filters['status'] ?? null) == $status)>{{ str_replace('_',' ',$status) }}</option>@endforeach</select></div>
<div class="col-xl-2 col-md-4"><label class="form-label">Desde</label><input type="date" name="from" value="{{ $filters['from'] ?? '' }}" class="form-control"></div><div class="col-xl-2 col-md-4"><label class="form-label">Hasta</label><input type="date" name="to" value="{{ $filters['to'] ?? '' }}" class="form-control"></div>
<div class="col-12 d-flex gap-2"><button class="btn btn-primary"><i class="bi bi-funnel me-1"></i>Aplicar filtros</button><a href="{{ route('dashboard') }}" class="btn btn-outline-secondary">Restablecer</a><a href="{{ route('indicators.index', request()->query()) }}" class="btn btn-outline-primary">Análisis de indicadores</a><a href="{{ route('intelligence', request()->query()) }}" class="btn btn-outline-primary ms-auto"><i class="bi bi-graph-up-arrow me-1"></i>Centro de Inteligencia</a></div>
</div></form>
<div class="row g-3 mb-4">@foreach([['Avance medio',$k['completion_average'] ?? 0,'%','bi-activity','primary'],['Cargas activas',$k['active'] ?? 0,'','bi-broadcast-pin','info'],['Pendientes de revisión',$k['review_pending'] ?? 0,'','bi-clipboard-check','warning'],['Próximas 72 h',$k['due_soon'] ?? 0,'','bi-alarm','warning'],['Reprogramadas',$k['reprogrammed'] ?? 0,'','bi-arrow-repeat','secondary'],['Con observación',$k['observed'] ?? 0,'','bi-chat-square-exclamation','danger']] as $card)<div class="col-6 col-xl-2"><div class="siget-kpi"><div class="siget-kpi-icon text-bg-{{ $card[4] }}"><i class="bi {{ $card[3] }}"></i></div><div><small>{{ $card[0] }}</small><strong>{{ $card[1] }}{{ $card[2] }}</strong></div></div></div>@endforeach</div>
<div class="row g-4"><div class="col-xl-7"><div class="card siget-card h-100"><div class="card-header"><h2>Ritmo de operación</h2><p>Entradas, cierres y capacidad de respuesta por periodo.</p></div><div class="card-body"><div class="siget-chart-wrap"><canvas id="monthlyTrendChart"></canvas></div></div></div></div><div class="col-xl-5"><div class="card siget-card h-100"><div class="card-header"><h2>Embudo de entregables</h2><p>Del trabajo programado a la validación/cierre.</p></div><div class="card-body"><div class="siget-chart-wrap"><canvas id="funnelChart"></canvas></div></div></div></div><div class="col-xl-6"><div class="card siget-card h-100"><div class="card-header"><h2>Capacidad por dirección</h2><p>Volumen y validación de entregables sin duplicar unidades.</p></div><div class="card-body"><div class="siget-chart-wrap"><canvas id="directionChart"></canvas></div></div></div></div><div class="col-xl-6"><div class="card siget-card h-100"><div class="card-header"><h2>Riesgo y oportunidad</h2><p>Vencimientos y desempeño por dependencia.</p></div><div class="card-body"><div class="siget-chart-wrap"><canvas id="agencyRiskChart"></canvas></div></div></div></div></div>
<div class="row g-4 mt-1"><div class="col-xl-7"><div class="card siget-card"><div class="card-header"><h2>Atención ejecutiva</h2><p>Casos que requieren decisión, seguimiento o corrección.</p></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Situación</th><th>Dependencia</th><th>Registro</th><th>Fecha</th></tr></thead><tbody>@forelse($analytics['risk_items'] ?? [] as $item)<tr><td><span class="badge text-bg-warning">{{ $item->status instanceof \BackedEnum ? $item->status->value : $item->status }}</span></td><td>{{ $item->agency?->name }}</td><td><a href="{{ route('loads.show',$item) }}"><strong>{{ $item->title }}</strong></a></td><td>{{ $item->effective_close_at?->format('d/m/Y') }}</td></tr>@empty<tr><td colspan="4" class="text-center py-4">Sin casos críticos en el alcance.</td></tr>@endforelse</tbody></table></div></div></div><div class="col-xl-5"><div class="card siget-card"><div class="card-header"><h2>Trazabilidad documental</h2><p>Evidencias vinculadas al universo seleccionado.</p></div><div class="card-body"><div class="siget-chart-wrap"><canvas id="evidenceChart"></canvas></div></div></div></div></div>
<script type="application/json" data-siget-chart="monthlyTrendChart">{!! json_encode($monthly) !!}</script>
<script type="application/json" data-siget-chart="funnelChart">{!! json_encode($funnel) !!}</script>
<script type="application/json" data-siget-chart="directionChart">{!! json_encode($unitPerformance) !!}</script>
<script type="application/json" data-siget-chart="agencyRiskChart">{!! json_encode($agencyPerformance) !!}</script>
<script type="application/json" data-siget-chart="evidenceChart">{!! json_encode($evidenceFunnel) !!}</script>
@endsection
